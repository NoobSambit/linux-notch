# Ordered implementation plan — begins only after product approval

**Status: planning only.** The architecture direction is approved; this revised product scope is not yet approved for implementation. Each milestone should be a small reviewable change with its own evidence. The design system starts early and is carried through subsequent work. Preserve inherited behavior and plugin compatibility by default; any removal needs an individual evidence record under [D14](DECISIONS.md). See [v1 specification](V1_SPEC.md) and [daily experience](V1_EXPERIENCE.md).

| Milestone | Goal and affected modules | Dependencies | Main risks | Validation / exit evidence |
| --- | --- | --- | --- | --- |
| 0. Baseline and rights inventory | Capture current Notch-like shell, dock/Hybrid, top/bottom, one/two monitors, fullscreen, media/notifications/clipboard/capture and plugin samples. Inventory bundled image/font/game asset provenance. `shell.qml`, `Dual`, Settings, sample plugins. | Approved product scope; live Hyprland session | Current docs are source-based, not a live multi-monitor/performance certificate | Versioned screenshots/video, interaction traces, hardware/Quickshell versions, idle/active resource readings, asset/license list. |
| 1. Regression and safety gates | Add focused tests for setting migration/invalid values, activity selection/restoration, plugin contract, IPC, Hyprland parsing, clipboard secrets and file URI semantics. Keep current validators/CI. Make weather/agent network use explicit and controllable; scope installer restarts to k4. `tools/prueba_*`, `.github/workflows/validar.yml`, existing Settings/network helpers and `instalar` only as justified. | Baseline | Freezing accidental behavior, changing saved privacy defaults, or building a large UI test framework | Existing gates plus small fixtures pass; offline/opt-in cases pass; installer cannot stop unrelated Quickshell sessions. |
| 2. Design system and core-surface polish | Establish original token roles and reusable QML states; render Notch/Island/Dock/Hybrid core surfaces, settings navigation and focus/hover/motion examples. Evolve `core/Theme.qml`, `core/Island*`, `api/K4/Tema.qml` compatibility layer and shared widgets incrementally. | Baseline, accessibility and performance measurements | Cloning reference visuals, breaking old plugin theming, costly blur/shaders | Side-by-side rendered original directions, dark/light, focus, text scale, reduced motion, frame/resource traces. Stable tokens and at least one polished core path before feature expansion. |
| 3. Geometry, modes and display behavior | Validate mode/edge/size/offset/reserve/fullscreen/preferred-display settings; reuse `shell.qml`, `services/Island.qml`, `plugins/Dual` and settings preview. Rehome on hotplug, keep one expanded owner. | Milestones 1–2 | Offscreen controls, focus/exclusive-zone bugs, mode transition breakage | One/two-monitor, hotplug, fullscreen per display, top/bottom, fractional-scale and bad-JSON recovery proof. |
| 4. Activity and interaction behavior | Extend current arbitration for compact/persistent/transient/user sessions, interruption/expiry/restoration; define hover/click/drag/shortcut/ESC rules. Polish existing media, volume, notifications, recording, battery, Wi-Fi/Bluetooth and known system status. `shell.qml`, `Notifs`, `Media`, `Audio`, `Captura`, host widgets; additive plugin mapping only if needed. | Milestones 1–3 | Second source of truth, focus theft, stale HUDs | Media→volume→media, notification during launcher, recording survives interruption, no stale replay, old plugin sample passes; rendered/focus traces. |
| 5. Dock and Hybrid polish | Reuse `Dual/Muelle` state and app model; align dock tasks/Hybrid activity with shared visual system, reduce default travel and support reduced motion. `plugins/Dual/*`, host/settings. | Milestones 2–4 | Hyprland focus/window polling, transition surfaces intercepting input | Pin/reorder/focus/multiple windows/drawer, reserve reflow, mode switch, one/two-monitor video and resource traces. |
| 6. Shelf DnD feasibility gate | Throwaway test fixture for Quickshell layer-shell file URI drag-in/out between actual Hyprland apps, including copy/move/cancel and keyboard fallback. No product integration yet. | Live Hyprland session, agreed Shelf ownership proposal | Wayland MIME/source limitations and unsafe move semantics | App/offer/target matrix and documented go/rescope decision. Any change to the product promise returns to user review. |
| 7. Local Shelf | Add staging UI/state, local metadata and small visible actions (open/reveal/copy path/remove; optional pin). Apply shared tokens and one-screen-at-a-time activity integration. New Shelf module, settings, host, local store. | Milestones 2–4, 6 | Data loss, stale paths, drag-only access, metadata privacy | Original untouched, copy/cancel, missing/unsupported source, keyboard actions, restart retention, no network traffic. |
| 8. Clipboard and file-action polish | Improve existing clipboard preview/search/pin/keyboard path; evaluate file-reference MIME before adding it. Define minimal extensible Shelf action contract; optional compression only if cheap. `services/Clipboard.qml`, `tools/portapapeles.py`, `plugins/Clipboard/*`, Shelf. | Milestones 2, 7; secret-handling tests | Storage churn, secret leakage, action API overdesign | Real text/image/password-manager cases, keyboard paths, file action/missing-source handling, no redundant watcher. |
| 9. Broader module and settings coherence | Map current settings, including the early network controls, into user-intent groups without breaking stable IDs/IPC; polish high-frequency launcher, notifications, controls and capture entry points using shared components. Add optional capture→Shelf handoff only if simple. `services/Settings.qml`, `plugins/Settings/*`, touched services/plugins/widgets. | Milestones 1–8 | Large settings migration and incidental rewrite of optional modules | Search finds old and new labels; old saved values/IPC still work; contrast/focus/motion review; untouched optional modules remain available. |
| 10. Compatibility, performance and release gate | Exercise built-in/external sample plugins, hot reload and failure paths; manual Hyprland install/dependency guide; measure idle/media/dock/Shelf CPU/RSS/GPU/frame behavior; resolve privacy, rights and release issues. `PluginManager`, `api/K4`, CI/docs/installers only as required. | Prior milestones | Asset rights, hidden watchers, GPU effects, Quickshell version drift | Exact CI gates, plugin fixtures, live 1/2-monitor QA, resource report with hardware, notices/assets inventory, release decision. |

## Stop/go rules

- Shelf stays a v1 proposal until the DnD gate verifies target apps. A failed gate requires measured evidence and user approval of a revised product contract; do not silently drop it.
- File-reference clipboard and brightness HUD are conditional on real MIME/device paths. Their absence does not block text/image clipboard polish or the other supported HUDs.
- Visual quality is checked throughout, starting at milestone 2; milestone 9 is broader adoption, not a last-minute skin. Rendered proof includes focus, monitor, fullscreen, reduced motion and resource behavior, not screenshots alone.
- A plugin compatibility failure blocks the proposed API migration, not the ability to retain the existing API. The narrow compositor seam is extracted only at repeated Hyprland couplings identified in [ARCHITECTURE.md](ARCHITECTURE.md), not as a separate framework rewrite.

## Highest risks to monitor

1. Cross-app Wayland file DnD into/out of a layer-shell window, particularly drag cancellation and copy/move semantics.
2. One global activity owner versus multi-monitor focus, fullscreen and persistent dock state.
3. Existing `Dual` travel surfaces/Hyprland commands and their input/GPU cost.
4. Plugin API/lifecycle compatibility while activity and visual contracts evolve; enabled external QML still runs in process.
5. Clipboard file-reference MIME and secret detection; current source supports text/image and heuristic exclusion, not a universal guarantee.
6. Theme/blur/shader cost, optional module watchers, network defaults and bundled asset rights.

Each milestone records changes, commands/tests, hardware/runtime context and unmet scenarios. Passing static validators alone does not prove the live product.
