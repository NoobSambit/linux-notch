# Ordered implementation plan — begins only after product approval

**Status: planning only.** Architecture and v1 direction are approved; implementation is not authorized by this document. Each milestone needs reviewable evidence. Shelf DnD proof comes immediately after baseline/safety; design-system work follows before feature expansion. Preserve inherited behavior by default, using the [support tiers](V1_SPEC.md#support-tiers-and-release-gates); any removal needs an individual evidence record under [D14](DECISIONS.md). The [asset audit](ASSET_LICENSE_AUDIT.md) is a release gate, not an excuse for silent deletion.

| Milestone | Goal and affected modules | Dependencies | Main risks | Validation / exit evidence |
| --- | --- | --- | --- | --- |
| 0. Baseline and rights inventory | Capture current surface/dock, top/bottom and compatibility-only sides, one/two monitors, fullscreen, notification owner, media/clipboard/capture and plugin samples. Start [asset audit](ASSET_LICENSE_AUDIT.md). | Live Hyprland session | Source research is not live proof | Versioned screenshots/video, hardware/Quickshell versions, idle/active resource readings and asset manifest gaps. |
| 1. Regression and safety gates | Focused settings/activity/plugin/IPC/Hyprland/clipboard fixtures; check current notification bus owner and conflict behavior. Make known network features disclosed/controllable and scope installer restarts to k4. Existing validators/CI, Settings/helpers and `instalar` only as justified. | Baseline | Changed saved defaults, daemon conflict, overbuilt tests | Existing gates plus targeted fixtures; offline/opt-in cases; no unrelated Quickshell termination; notification preflight evidence. |
| **2. Isolated Shelf DnD feasibility** | Throwaway Quickshell layer-shell target outside product modules. Test at least two available common Hyprland file managers (for example Thunar and Dolphin/Nautilus) dragging files/folders/multiple files in via `text/uri-list`; drag out to another app; inspect copy/move negotiation, cancellation, missing source, keyboard fallback and same/multi-monitor behavior where practical. | Milestones 0–1; live Hyprland session | Quickshell/Qt drag-source or layer-shell limitations | App/version/MIME/action matrix, repro steps, source integrity and exact go/rescope conclusion. No product integration. |
| 3. Design system and core-surface polish | Establish original tokens/reusable states and render Notch/Island/Dock/Hybrid, settings navigation and focus/hover/motion examples. Evolve `core/Theme.qml`, `core/Island*`, `api/K4/Tema.qml` compatibility and shared widgets incrementally. | Baseline; Shelf feasibility outcome known | Reference copying, plugin theme break, blur cost | Original rendered directions, dark/light, focus, text scale, reduced motion and frame/resource traces. |
| 4. Geometry, modes and display behavior | Validate top/bottom mode/size/offset/reserve/fullscreen/preferred-display settings; reuse `shell.qml`, `Island`, `Dual` and preview. Rehome on hotplug. Existing sides remain compatibility-only pending tests. | Milestones 1–3 | Offscreen focus/exclusive zones | One/two-monitor, hotplug, fullscreen, fractional scale and bad-JSON recovery proof. |
| 5. Host Activity Policy and interaction | Extend existing arbitration for compact/persistent/transient/user sessions and hover/click/drag/shortcut/ESC rules. Integrate media, volume, recording, battery, Wi-Fi/Bluetooth and notifications **only when this app owns the notification D-Bus name**. `shell.qml`, `Notifs`, host widgets and affected services. | Milestones 1, 3–4; notification policy [D20](DECISIONS.md) | Duplicate owner, focus theft, stale HUDs | Media→volume→media, no stale replay, recording visible, old plugin passes; Own and External notification modes tested against a real daemon. |
| 6. Dock and Hybrid polish | Reuse `Dual/Muelle` state; apply shared visual system, shorter default travel and reduced motion. `plugins/Dual/*`, host/settings. | Milestones 3–5 | Hyprland focus/window polling and transition input cost | Pin/reorder/focus/drawer, reserve reflow, mode switch and resource traces. |
| 7. Local Shelf | Add local staging, keyboard route and open/reveal/copy-path/remove actions with shared tokens. New Shelf module, settings, host/local store. | Milestones 2–5; positive Shelf gate | Data loss, missing paths, metadata privacy | Original untouched, copy/cancel, missing/unsupported source, restart retention; Shelf actions themselves make no network request. |
| 8. Clipboard and file-action polish | Improve current clipboard previews/search/pin/keyboard path, evaluate file-reference MIME, keep the Shelf action contract small. `Clipboard` service/helper/view and Shelf. | Milestones 3, 7; secret tests | Storage churn, secret leak, overdesign | Text/image/password-manager cases, keyboard actions, missing-source behavior and no duplicate watcher. |
| 9. Broader module/settings coherence | Regroup saved settings by intent without breaking IDs/IPC; apply shared components to launcher, notification center in Own mode, controls and capture entry points. Optional capture→Shelf handoff only if simple. | Milestones 1–8 | Incidental redesign of supported inherited modules | Search/aliases work; saved values survive; focus/contrast/motion; supported inherited smoke checks and no regressions in changed optional modules. |
| 10. Compatibility, performance and release gate | Exercise plugin samples/hot reload, manual Hyprland dependency guide, measured CPU/RSS/GPU/frame behavior, notification owner switching, privacy and [asset/data/IP audit](ASSET_LICENSE_AUDIT.md). | Prior milestones | Rights, hidden watchers, bus-owner races | Exact CI and live QA; tier-based release gates; required notices/provenance or exclusion of unresolved payloads; release decision. |

## Stop/go rules

- The **milestone 2 Shelf spike runs before design-system/geometry/activity work** and remains separate from product implementation. A failed gate records the exact limitation and returns Shelf scope for product review; no silent workaround or removal.
- Notification delivery/history/actions require this app to own `org.freedesktop.Notifications`. External mode must not impersonate another daemon or promise a mirrored notification center. Detect conflicts, explain degraded capability, and do not kill the existing daemon.
- File-reference clipboard and brightness HUD are conditional on real MIME/device paths. Their absence does not block text/image clipboard polish or the other supported HUDs.
- Visual quality is checked throughout, starting at milestone 3; milestone 9 is broader adoption, not a last-minute skin. Rendered proof includes focus, monitor, fullscreen, reduced motion and resource behavior, not screenshots alone.
- A plugin compatibility failure blocks the proposed API migration, not the ability to retain the existing API. The narrow compositor seam is extracted only at repeated Hyprland couplings identified in [ARCHITECTURE.md](ARCHITECTURE.md), not as a separate framework rewrite.

## Highest risks to monitor

1. Cross-app Wayland file DnD into/out of a layer-shell window, particularly drag cancellation and copy/move semantics.
2. One global activity owner versus multi-monitor focus, fullscreen and persistent dock state.
3. Existing `Dual` travel surfaces/Hyprland commands and their input/GPU cost.
4. Plugin API/lifecycle compatibility while activity and visual contracts evolve; enabled external QML still runs in process.
5. Clipboard file-reference MIME and secret detection; current source supports text/image and heuristic exclusion, not a universal guarantee.
6. Theme/blur/shader cost, optional module watchers, network defaults and bundled asset rights.

Each milestone records changes, commands/tests, hardware/runtime context and unmet scenarios. Passing static validators alone does not prove the live product.
